{
  "private": true,
  "scripts": {
    "mkdist": "mkdir dist",
    "postinstall": "npm run mkdist",
    "fable:clean": "dotnet fable clean -o dist --yes",
    "fable:build": "dotnet fable src/ExampleProject.fsproj -o dist",
    "fable:deploy": "npm run fable:clean && npm run fable:build",
    "fable:watch": "npm run fable:build -- --watch",
    "vite:watch": "vite",
    "vite:build": "vite build",
    "vite:rundeploy": "cd deploy && vite",
    "dev": "concurrently \"npm:*:watch\"",
    "deploy": "npm run fable:deploy && npm run vite:build"
  },
  "dependencies": {
    "bulma": "^0.9.3",
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "vite": "^4.4.9"
  }
}
